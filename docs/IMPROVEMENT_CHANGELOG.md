# Improvement Changelog

Every row below records an actual implementation stage preserved in Git history. Correctness
figures use End-to-End Semantic Repair Rate (E2E-SRR); a repair counts only when all 48 sealed
vectors pass, the source remains unchanged, and the formula diff is minimal. Revision 1 and
revision 2 are different frozen benchmarks, so their scores are historical checkpoints rather
than a causal comparison across revisions.

| Stage | What we tried and why | Evidence | Decision / learning |
|---|---|---|---|
| Baseline (`d44e78c`, benchmark v1) | A direct agent scanned formulas once and applied at most one textual policy substitution. This represented the simplest plausible automated audit. | 50.0% E2E-SRR (6/12), 100% clean preservation, 0% on H01. Historical evidence is stored in `d44e78c:evals/results.json`. | Established the starting point, but later review found that its substitution table encoded benchmark-specific defect fragments. |
| Iteration 1 (`d44e78c`, benchmark v1) | Added cited typed rules, policy-derived boundary cases, a fail-closed formula worker, dependency/Ochiai localization, constrained repair, approval, and sealed replay. | 100% E2E-SRR (12/12), 100% clean preservation, 100% on H01: +50 percentage points over the v1 baseline. | Kept the staged witness workflow. The perfect result triggered an adversarial audit of the benchmark and evaluator rather than an immediate completion claim. |
| Iteration 2 (`be16d1e`, benchmark v2) | Rebuilt lookup mutants around real ordered `LOOKUP`, corrected proportional proration, and made all 48 held-out inputs disjoint from visible inputs. This tested whether the result survived a more realistic benchmark. | ClauseGrid remained at 100% E2E-SRR and 100% clean preservation; the direct baseline fell to 33.3%. `artifacts/benchmark-validation.json` proves every mutant is killed and the split is disjoint. | Kept benchmark v2. The lower baseline score is not presented as an agent improvement; it is evidence that v1 was too forgiving. |
| Iteration 3 (`be16d1e`, benchmark v2) | Removed benchmark-specific substitution logic and gold-formula filtering, moved the oracle and held-out cases to `evals/sealed`, and ran each agent with a file-capability guard. | Hardened result: 33.3% baseline versus 100% ClauseGrid (+66.7 pp), both with 100% clean preservation. `tests/evaluation/test_oracle_isolation.py` proves the repair workers cannot import or read evaluator-only data. | Kept. Evaluation integrity mattered more than preserving the earlier baseline score. |
| Iteration 4 (`5a66c74`, benchmark v2) | Made trajectories judge-readable, bound approval to repaired workbook bytes, made new OOXML entries deterministic, and visually fixed formula overflow in the UI. | 33 tests pass; trajectories contain instructions, tools, responses, feedback, retries, and checkpoints; independent approved runs produce the same workbook and approval hash; all 17 workbooks pass the renderer. E2E-SRR stayed 100%. | Kept. These changes improved reproducibility and end-to-end quality without changing the scored outcome. |
| Final | Combined the hardened witness workflow, sealed evaluator, human approval, reproducible artifacts, and explicit limitations. | 33.3% baseline versus 100% ClauseGrid (+66.7 pp), 100% clean preservation, and H01 improved from 0% to 100%. | The main contribution is not formula generation alone; it is the reviewable chain from cited policy to counterexample, localization, minimal patch, approval, and independent replay. |
| Agent rebuild (unscored) | Replaced fixed role-labelled stages with an OpenAI-compatible model-controlled manager, generic tools, fresh-context falsifier, raw traces, budgets, and separate approval. | Scripted behavior tests prove falsifier feedback can force a second candidate. A live NIM GPT-OSS smoke produced a proposal after 17 manager turns, 10 falsifier turns, 27 tool calls, and six sandbox executions. | Kept. This establishes real agent mechanics, but no blind agent benchmark has been run; the earlier 33.3%/100% figures remain legacy deterministic results only. |
| Provider hardening (unscored) | Stress-tested live providers and fixed premature token/tool exhaustion, oversized parallel observations, empty terminal streams, lost citation handles after compaction, ignored mandatory tool choice, empty chat completions, and ignored serial-tool settings. | Offline transport/loop tests cover each failure class. OpenCode free-model runs reached up to 33 tool calls but none completed candidate staging and falsification; the traces ended in bounded `ABSTAIN` states. | Kept the provider-neutral fixes. A compatibility run is not a repeated benchmark, and the tested OpenCode free endpoints are not a production recommendation. |
| Failure-directed controller hardening (unscored) | Replayed NVIDIA Lightning against M10 and converted observed failures into provider-neutral controls: semantic read caching, impossible-action filtering, forced manager/falsifier experiments, evidence-aware coordination, quote-safe and structural edits, candidate state transitions, capped failed experiment loops, and reserved terminal turns. | Diagnostics progressed from discovery-only abstention to candidate staging/falsification. The latest completed run registered citations and two experiments, then safely `ABSTAIN`ed after mandatory-tool retries returned plain text (19 manager turns, 752.1 s, no patch). Tests cover each controller transition and fail-closed rejection. | Kept. Deterministic orchestration owns budgets and safety progress, while the model still chooses hypotheses, evidence queries, experiment inputs, repairs, and verdict arguments. Lightning remains task-unreliable rather than being promoted from one partial run. |
| Repeated agent evaluation and public demo (unscored) | Added a 30-run default manager/falsifier-versus-single-agent harness and a constrained internet-facing deployment path. | Exact-hash evaluation approval, post-run sealed-oracle access, Wilson intervals, fairness metadata, non-root Docker execution, async jobs, same-origin/Host checks, rate limiting, administrator-only approval, and excluded sealed evaluator data are covered by tests and container smoke checks. | Kept. Deployment makes the synthetic demonstration reviewable; it does not turn a single live run or the legacy deterministic score into a production accuracy claim. |
| Qubrid/GLM compatibility and rejection recovery (unscored) | Added a first-class Qubrid preset and tested the free `zai-org/GLM-4.7-Flash` model. Converted repeated rejected candidate/no-change actions into bounded evidence recovery or escalation. | Named/required tool probes passed. Two full M10 runs safely `ABSTAIN`ed after 1,009.3 s and 1,342.9 s; neither reached falsification. The second run proved the new candidate recovery, candidate/no-op guards, experiment cap, no-change evidence gate, and source preservation. | Kept the provider and generic recovery controls. Eliminated this model for current M10 task accuracy despite zero catalog token price. |
| Qubrid model replacement and stale-tool recovery (unscored) | Tested GPT-OSS 120B, Nemotron Nano variants, Qwen Coder, Kimi Code, and DeepSeek V3.2 against the same mandatory function contract; selected DeepSeek as the first correct Qubrid candidate. Added bounded correction when a model calls a tool removed from the current controller state. | GPT-OSS emitted empty tool arrays through Qubrid. DeepSeek passed both tool probes; its first M10 run exposed stale tool reuse, and the repaired run identified the P6 defect but exhausted 30 turns after invalid experiments without staging or falsification. Focused and full tests cover recovery and exhaustion. | Kept the provider-neutral recovery and DeepSeek integration default. Did not promote any Qubrid model to task-qualified or production status. |
| Private real-input path (unscored) | Replaced the browser's synthetic-only input restriction with explicit `.xlsx` plus governing `.pdf` upload for local/private use. Added compatibility preflight, exact hashes, consent, temporary isolation, single-use ownership, expiry, and upload-aware approval cleanup. | A browser smoke staged the real M10 workbook/policy pair (22 formulas, four sheets, four pages), ran on their exact hashes, and produced no console errors. Automated tests cover malformed PDFs, unsupported formula semantics, cross-sheet constraints/qualified inputs, replay rejection, expiry, ABSTAIN cleanup, and REPAIR approval/download cleanup. | Kept. Custom files now exercise the genuine generic manager/falsifier runtime, while the synthetic suite remains the only frozen scored benchmark. The anonymous public demo remains synthetic-only until authenticated tenant isolation exists. |
| Public evidence handoff and guarded uploads (unscored) | A live M10 run registered four citations but lost their quote text from the compact terminal ledger, then incorrectly said the policy was unreadable. Added bounded quote previews, hid runtime identity from anonymous responses, and deliberately exposed the existing upload path on Render for public feedback. | Regression tests require terminal/coordination notices to retain policy text, public JSON to omit provider/model fields, and a same-origin public workbook/policy pair to complete hash-bound preflight and cleanup. A fresh live run retained RB-202/RB-203 through falsification and correctly rejected an unsafe candidate, but still exhausted its manager budget and `ABSTAIN`ed; live upload preflight accepted 22 formulas, four sheets, and four policy pages. | Kept for the time-boxed demo. Public upload exposure is explicitly not multi-tenant production readiness; confidential inputs remain out of scope and browser approval stays disabled. The evidence-loss fix is verified, but the deployed model remains task-unreliable. |
| Approved Policy Pack replay (unscored) | Separated optional model investigation from the trusted recurring control. Added a committed dual-role demo release, generated boundary cases, a retained waiver regression, an independent Decimal/date oracle, and separate pack/suite/mapping hashes. | Targeted tests compare the oracle with the sealed reference, prove stable hashes and zero model calls, pass the pristine workbook, and require all 12 public formula mutants to fail the approved suite. | Kept. The durable differentiator is approved executable meaning and repeatable evidence, not another model opinion. Public governance remains read-only until authenticated durable storage exists. |
| Demo-first workbench and guided onboarding (unscored) | Replaced the long card stack with a full-width policy-assurance workbench, separated recurring verification from optional AI investigation, added a first-screen M10 quick check, and built a seven-step accessible spotlight tour. | Browser checks exercised the M10 quick path, exact 26/27 evidence, mode and source keyboard navigation, result guidance, tour navigation/dismissal, and mode restoration. The UI suite passes 32 tests and the full suite passes 307 tests. | Kept. First users can now understand and run the differentiating zero-model control before reading supporting material, while AI-only workflow state stays out of recurring verification. |
| Submission evidence hardening (unscored) | Replaced the submission's model-agent evidence gap with a successful live schema-v3 manager/falsifier trajectory and made the Claude comparison directly discoverable from the hero and top navigation. | The selected M10 run contains 132 hash-chained events, 18 manager turns, 8 falsifier turns, 33 tool calls, nine sandbox executions, a one-cell P6 proposal, and five independent falsifier experiments. `verify-trajectory` reproduces its final hash. | Kept. The judges can now distinguish the model-directed agent workflow from the older deterministic benchmark and independently verify both evidence chains. |
| Live agent activity and readable repair review (unscored) | Added a bounded public activity stream so users can see the manager and falsifier call tools while an investigation runs. Reformatted model explanations into normal paragraphs and isolated Excel formulas instead of showing one large raw paragraph. | Public events contain only role, turn, elapsed time, tool name, and success state; arguments, observations, model identity, and hidden reasoning are excluded. Scripted integration proves a tool-call event arrives before its result, a browser smoke rendered the timeline, and the complete suite passes 311 tests. | Kept. Observable actions make the agent loop understandable without turning private reasoning or workbook evidence into a browser data leak. |

## Removed experiment: benchmark-specific substitutions and gold-formula localization

The first baseline contained a fixed table of formula fragments such as `H6<=0.95` and
`J6>1`, while the first localizer discarded cells whose formulas already matched a compiler-built
reference. Those shortcuts made the benchmark easier by encoding knowledge of how its mutants
were constructed. They were removed rather than optimized.

After removal, the hardened baseline scored 33.3% on benchmark v2 while the advanced workflow
still scored 100%. The v1 and v2 figures are not directly comparable because the benchmark also
changed; the decisive evidence is structural: current repair processes cannot import evaluator
modules, hidden cases, mutation descriptions, or pristine formulas. This experiment taught us
that a high score is not credible if localization or the baseline can recognize the test generator.

## Challenging case: H01

H01 combines three defects that interact downstream: ordered tier lookup (`N6`), waiver scope
(`P6`), and cap order (`S6`). The direct baseline returned `NO_CHANGE`, passed only 9 of 48 sealed
vectors, and failed first at H10. ClauseGrid used 140 of its 160 allowed case executions,
changed exactly `N6`, `P6`, and `S6`, passed all 48 sealed vectors, preserved the source, and
changed no unrelated formula.

H01 revealed that a one-shot edit can appear locally reasonable while leaving interacting faults
hidden. The useful orchestration is sequential evidence-driven repair with complete replay after
each accepted candidate, bounded by a strict change and execution budget.

## Main failure mode and hot take

The main failure mode is ambiguous or conflicting policy language. ClauseGrid must abstain
until a qualified reviewer supplies an interpretation, because generating an expected result would
otherwise fabricate certainty. See [FAILURE_MODE.md](FAILURE_MODE.md).

**A spreadsheet returning a number is not evidence that it implements the policy.** The evidence
is the full witness chain: cited rule, discriminating input, observed divergence, dependency path,
minimal patch, accountable approval, and independent replay.

## Zero-price provider retest — 2026-08-30

Retested all three models that Qubrid currently reports at zero input and output price. GLM-4.7
Flash and GLM-4.6V Flash failed closed on repeated provider rate limits; GLM-4.5 Flash spent 690.7
seconds exploring four experiments before returning malformed tool JSON. None staged a candidate or
reached the falsifier. The result was rejected for deployment: cost alone is not a useful model
selection criterion when the full correctness and reproducibility contract does not complete.
